<h1 class="text-lg font-semibold text-indigo-700 mb-4">Déclaration TVA</h1>

<div class="bg-white rounded-lg shadow p-6 max-w-2xl">
  <dl class="grid grid-cols-2 gap-4 mb-6">
    <div>
      <dt class="text-xs font-medium text-gray-500">Période</dt>
      <dd class="text-sm text-gray-900">
        <%= Accounting::DatePresenter.new(@declaration.period_start).format %> —
        <%= Accounting::DatePresenter.new(@declaration.period_end).format %>
      </dd>
    </div>
    <div>
      <dt class="text-xs font-medium text-gray-500">Type</dt>
      <dd class="text-sm text-gray-900"><%= @declaration.period_type %></dd>
    </div>
    <div>
      <dt class="text-xs font-medium text-gray-500">Statut</dt>
      <dd class="text-sm text-gray-900"><%= @declaration.status %></dd>
    </div>
  </dl>

  <% if @declaration.grids.any? %>
    <h2 class="text-sm font-semibold text-gray-700 mb-3">Grilles TVA</h2>
    <table class="w-full text-sm border-collapse">
      <thead>
        <tr class="bg-gray-50 border-b border-gray-200">
          <th class="px-3 py-2 text-left text-xs font-medium text-gray-500">Code grille</th>
          <th class="px-3 py-2 text-right text-xs font-medium text-gray-500">Montant (€)</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        <% @declaration.grids.sort.each do |code, amount| %>
          <tr>
            <td class="px-3 py-2 font-mono text-gray-700"><%= code %></td>
            <td class="px-3 py-2 text-right font-mono text-gray-900">
              <%= Accounting::MoneyPresenter.new(BigDecimal(amount)).format %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="text-sm text-gray-500">Aucune grille TVA pour cette période.</p>
  <% end %>
</div>

<div class="mt-4">
  <%= link_to 'Retour aux déclarations', accounting_vat_declarations_path,
        class: 'text-sm text-indigo-600 hover:underline' %>
</div>
