<h1 class="text-lg font-semibold text-indigo-700 mb-4">New VAT Declaration</h1>

<%= form_with model: @declaration, scope: :accounting_vat_declaration,
              url: accounting_vat_declarations_path,
              class: 'max-w-lg' do |f| %>

  <div class="mb-4">
    <%= f.label :fiscal_year_id, 'Fiscal year', class: 'block text-sm font-medium text-gray-700 mb-1' %>
    <%= f.collection_select :fiscal_year_id, @fiscal_years, :id,
          ->(fy) { "#{fy.start_date.year}" },
          { include_blank: false },
          { class: 'block w-full rounded-md border-gray-300 text-sm' } %>
  </div>

  <div class="mb-4">
    <%= f.label :period_type, 'Period type', class: 'block text-sm font-medium text-gray-700 mb-1' %>
    <%= f.select :period_type,
          [['Monthly', 'monthly'], ['Quarterly', 'quarterly']],
          { include_blank: false },
          { class: 'block w-full rounded-md border-gray-300 text-sm' } %>
  </div>

  <div class="mb-4">
    <%= f.label :period_start, 'Period start', class: 'block text-sm font-medium text-gray-700 mb-1' %>
    <%= f.date_field :period_start, class: 'block w-full rounded-md border-gray-300 text-sm' %>
  </div>

  <div class="mb-6">
    <%= f.label :period_end, 'Period end', class: 'block text-sm font-medium text-gray-700 mb-1' %>
    <%= f.date_field :period_end, class: 'block w-full rounded-md border-gray-300 text-sm' %>
  </div>

  <div class="flex gap-3">
    <%= f.submit 'Generate declaration',
          class: 'px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-md hover:bg-indigo-700' %>
    <%= link_to 'Cancel', accounting_vat_declarations_path,
          class: 'px-4 py-2 text-sm text-gray-600 hover:text-gray-800' %>
  </div>

<% end %>
