<% content_for :breadcrumb do %>
  <%= render Ui::BreadcrumbsComponent.new(items: [
    { label: 'Dashboard', path: accounting_root_path },
    { label: 'VAT Declarations' }
  ]) %>
<% end %>

<%= render Ui::PageHeaderComponent.new(title: 'VAT Declarations') do |header| %>
  <% if policy(Accounting::VatDeclaration).create? %>
    <% header.with_actions do %>
      <%= link_to 'New declaration', new_accounting_vat_declaration_path,
            class: 'inline-flex items-center px-4 py-2 bg-primary-600 text-white text-sm font-medium rounded-md hover:bg-primary-700 transition-colors' %>
    <% end %>
  <% end %>
<% end %>

<%= render 'shared/filters', fields: [
  { name: :fiscal_year_id, label: 'Fiscal year', type: :select, options: Accounting::FiscalYear.order(year: :desc).map { |f| [f.year, f.id] } }
] %>
<% if @declarations.empty? && !filters_active? %>
  <%= render Ui::EmptyStateComponent.new(
        title: 'No VAT declarations yet',
        description: 'Submit your first VAT declaration for the current period.',
        icon: :clipboard
      ) do |state| %>
    <% if policy(Accounting::VatDeclaration).create? %>
      <% state.with_actions do %>
        <%= link_to 'New declaration', new_accounting_vat_declaration_path,
              class: 'inline-flex items-center px-4 py-2 bg-primary-600 text-white text-sm font-medium rounded-md hover:bg-primary-700 transition-colors' %>
      <% end %>
    <% end %>
  <% end %>
<% else %>
  <%= render 'shared/clear_column_filters' %>
  <div class="bg-white rounded-lg border border-gray-200 overflow-x-clip">
    <table class="w-full text-sm">
      <thead class="bg-gray-50 border-b border-gray-200">
        <tr>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= column_header(Accounting::VatDeclaration, :period_start, 'Period', resource: 'vat_declarations') %></th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= column_header(Accounting::VatDeclaration, :period_type, 'Type', resource: 'vat_declarations') %></th>
          <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= column_header(Accounting::VatDeclaration, :status, 'Status', resource: 'vat_declarations') %></th>
          <th class="px-4 py-3"></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        <% @declarations.each do |decl| %>
          <tr class="hover:bg-gray-50 transition-colors">
            <td class="px-4 py-3 text-gray-700">
              <%= Accounting::DatePresenter.new(decl.period_start).format %> —
              <%= Accounting::DatePresenter.new(decl.period_end).format %>
            </td>
            <td class="px-4 py-3 text-gray-500 capitalize"><%= decl.period_type %></td>
            <td class="px-4 py-3 text-gray-500"><%= decl.status %></td>
            <td class="px-4 py-3 text-right">
              <%= link_to 'View', accounting_vat_declaration_path(decl),
                    class: 'text-primary-600 hover:text-primary-800 text-sm font-medium' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <%= render 'shared/pagination', pagy: @pagy %>
<% end %>
<%= turbo_stream_from ActsAsTenant.current_tenant, :vat_declarations %>
