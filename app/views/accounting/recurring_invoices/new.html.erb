<% content_for :breadcrumb do %>
  <%= render Ui::BreadcrumbsComponent.new(items: [
    { label: 'Dashboard', path: accounting_root_path },
    { label: 'Recurring invoices', path: accounting_recurring_invoices_path },
    { label: 'New' }
  ]) %>
<% end %>

<%= render Ui::PageHeaderComponent.new(title: 'Make recurring', back_path: accounting_recurring_invoices_path, back_text: 'Recurring invoices',
      description: 'A draft copy of the invoice is created at each due date, for you to review and post. Nothing is posted or sent automatically.') %>

<%= render 'form', recurring_invoice: @recurring_invoice %>
