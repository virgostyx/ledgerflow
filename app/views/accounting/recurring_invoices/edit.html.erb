<% content_for :breadcrumb do %>
  <%= render Ui::BreadcrumbsComponent.new(items: [
    { label: 'Dashboard', path: accounting_root_path },
    { label: 'Recurring invoices', path: accounting_recurring_invoices_path },
    { label: 'Edit' }
  ]) %>
<% end %>

<%= render Ui::PageHeaderComponent.new(title: 'Edit recurring invoice', back_path: accounting_recurring_invoices_path, back_text: 'Recurring invoices') %>

<%= render 'form', recurring_invoice: @recurring_invoice %>
