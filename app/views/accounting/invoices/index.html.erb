<h1>Factures</h1>

<% if policy(Accounting::Invoice).create? %>
  <%= link_to 'Nouvelle facture', new_accounting_invoice_path %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Numéro</th>
      <th>Type</th>
      <th>Partenaire</th>
      <th>Date</th>
      <th>Total TTC</th>
      <th>Statut</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @invoices.each do |invoice| %>
      <% presenter = Accounting::InvoicePresenter.new(invoice) %>
      <tr>
        <td><%= presenter.invoice_number_or_draft %></td>
        <td><%= presenter.type_label %></td>
        <td><%= invoice.partner.name %></td>
        <td><%= presenter.formatted_date %></td>
        <td><%= presenter.formatted_total %></td>
        <td><%= presenter.status_label %></td>
        <td>
          <%= link_to 'Voir', accounting_invoice_path(invoice) %>
          <% if policy(invoice).update? %>
            <%= link_to 'Modifier', edit_accounting_invoice_path(invoice) %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
