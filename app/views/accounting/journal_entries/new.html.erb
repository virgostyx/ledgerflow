<h1 class="text-lg font-semibold text-indigo-700 mb-6">Nouvelle écriture</h1>

<% if flash[:alert] %>
  <div class="mb-4 p-3 bg-red-50 text-red-700 rounded-lg text-sm"><%= flash[:alert] %></div>
<% end %>

<%= render ::Layouts::JournalEntryFormComponent.new(
      entry:    @entry,
      journals: @journals,
      accounts: @accounts
    ) %>
